parameters:
  - name: assetid
    in: path
    required: true
    type: string
    description: asset id
responses:
  200:
    description: Get ownership for given asset id
    schema:
        type: array
        items: json